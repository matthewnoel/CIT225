#include<ctime>
#include<cstdlib>
#include"QueueLinkedList.h"

int main() {
        int randNum;
        srand(time(NULL));

        for (int i = 0; i < 5; i++) {
                randNum = rand() % 89 + 10;
                //add num to queue
        }
        return 0;
}